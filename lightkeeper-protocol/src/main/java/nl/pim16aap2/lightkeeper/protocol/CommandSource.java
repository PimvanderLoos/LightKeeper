package nl.pim16aap2.lightkeeper.protocol;

/**
 * Identifies the actor that executes a server-side command.
 */
public enum CommandSource
{
    /**
     * Commands run as the server console sender.
     */
    CONSOLE
}
